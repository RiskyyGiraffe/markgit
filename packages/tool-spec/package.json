{
  "name": "@markgit/tool-spec",
  "version": "0.1.0",
  "description": "Open manifest schema for tools listed on Markgit",
  "files": ["markgit-tool.schema.json", "README.md"],
  "license": "Apache-2.0",
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "node -e \"JSON.parse(require('node:fs').readFileSync('markgit-tool.schema.json'))\"",
    "test": "pnpm build"
  }
}
